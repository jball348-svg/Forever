Add a `src/queue.js` module implementing a simple async task queue with concurrency control. It should export a `createQueue(concurrency)` function that returns a queue object with: `add(fn)` to enqueue an async task (returns a Promise that resolves with the task result), and `size()` to return the number of pending tasks. The queue should run at most `concurrency` tasks simultaneously. Add `tests/queue.test.js` verifying concurrency limits and correct result resolution. Add to npm test script.