Add a `src/retry.js` module that exports `retry(fn, options)` — an async function that calls `fn()` and retries up to `options.attempts` times (default 3) if it throws, waiting `options.delayMs` milliseconds (default 100) between attempts. It should return the result on success or throw the last error if all attempts fail. Add `tests/retry.test.js` verifying: success on first try, success after N retries, and failure after exhausting all attempts. Add to npm test script.