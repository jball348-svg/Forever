Add a `src/stateManager.js` module that builds on top of `src/store.js` and `src/eventBus.js`. It should export `setState(key, value)` (stores the value and emits a 'change' event with `{ key, value }`), `getState(key)`, and `subscribe(handler)` (registers a handler for all 'change' events). Add `tests/stateManager.test.js` to verify that calling setState triggers subscribed handlers with correct data. Add to npm test script.