Add a `src/decorators.js` module exporting higher-order function decorators: `readonly(obj, key)` which uses Object.defineProperty to make a property non-writable, `logged(fn, label?)` which wraps a function so every call is logged (using src/logger.js) with its label, arguments, and return value, and `timed(fn, label?)` which wraps a function and logs how long it takes to execute. Add `tests/decorators.test.js` verifying all three. Add to npm test script.