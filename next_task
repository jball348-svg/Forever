Refactor index.js to be more structured. Instead of a series of console.log statements, create a small module that exports a `greet()` function which builds and returns the welcome message as a string, then call it from index.js and print the result. This introduces basic modular design to the project.