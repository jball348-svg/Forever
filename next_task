Add a `src/cache.js` module implementing a TTL-based (time-to-live) in-memory cache. It should export: `set(key, value, ttlMs)` to store a value that expires after ttlMs milliseconds, `get(key)` to return the value or undefined if missing/expired, `has(key)` to check if a non-expired entry exists, and `delete(key)` to remove an entry. Add `tests/cache.test.js` using assert and setTimeout to verify TTL expiry. Add to npm test script.