Add a `src/eventBus.js` module that implements a minimal publish/subscribe event system. It should export `on(event, handler)` to register a listener, `off(event, handler)` to remove one, and `emit(event, data)` to call all listeners for that event. Then add a `tests/eventBus.test.js` to verify basic on/emit/off behaviour using assert.