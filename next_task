Add a `src/pool.js` module implementing a generic object pool. It should export `createPool(factory, options)` where factory is a function that creates a new object, and options includes `max` (maximum pool size, default 10). The returned object should have: `acquire()` returning a Promise that resolves with a pooled object (creating one if the pool isn't empty or max not reached), `release(obj)` to return an object to the pool, and `size()` to return the number of idle objects. Add `tests/pool.test.js` verifying acquire, release, and that max is respected. Add to npm test script.