Add a `src/config.js` module that centralises project configuration. It should export a plain object with at minimum: `name` (project name), `version` (read from package.json), and `startedAt` (ISO timestamp of when the process started). Other modules (e.g. logger, greet) should remain unchanged, but index.js should import config and log the project name and version using the logger on startup.