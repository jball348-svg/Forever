Add a TypeScript type definitions file for the Forever library. Create a `types/index.d.ts` file that provides full TypeScript typings for every public module exported from `src/index.js`. Requirements: 1) Typed declarations for all exported functions, classes, and objects including: cache, store, eventBus, logger, utils, debounce, throttle, memoize, pipeline, decorators, middleware, di, pool, stateManager, queue, retry, stream, commandBus, fsm, observable, router, scheduler, validator, config, greet, performance, and plugins. 2) Use proper generic types where applicable (e.g. memoize<T>, createPool<T>, createQueue<T>). 3) Export all types and interfaces that consumers may need (e.g. Plugin, PluginManager, BenchmarkResult). 4) Add a `"types"` field to `package.json` pointing to `types/index.d.ts`. 5) Create a `types/README.md` explaining how to use the types in a TypeScript project. 6) Validate the structure by creating a `types/example.ts` file that imports and uses the typed API to demonstrate correct usage.