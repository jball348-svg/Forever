Introduce a `src/pipeline.js` module that implements a simple function pipeline. It should export a `pipeline(...fns)` function that takes one or more functions and returns a new function. When the returned function is called with a value, it passes that value through each function in sequence (left to right), returning the final result. Add `tests/pipeline.test.js` using assert with a few examples (e.g. number transformations, string transformations). Add to npm test script.