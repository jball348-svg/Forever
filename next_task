Add a `src/debounce.js` module exporting two functions: `debounce(fn, delayMs)` — returns a function that delays invoking fn until delayMs ms have elapsed since the last call, and `throttle(fn, intervalMs)` — returns a function that invokes fn at most once per intervalMs ms regardless of how often it is called. Add `tests/debounce.test.js` using setTimeout/Promise to verify both behaviours. Add to npm test script.