Add a feature flags / toggles module. Create `src/featureFlags.js` with the following:

1) `createFlagStore(options?)` - factory function. Options:
   - `flags` (object, default {}) - initial flag definitions: each key is a flag name, each value is a flag config: `{ enabled: bool, rolloutPercentage?: number (0-100), description?: string, metadata?: object }`
   - `onChange(flagName, newValue, oldValue)` - optional callback when a flag changes

2) Returns an object with:
   - `isEnabled(flagName, context?)` - returns true if the flag is enabled. If `rolloutPercentage` is set, uses a stable hash of `context.userId` (or a random id) to deterministically place the user into or out of the rollout. Returns false for unknown flags.
   - `enable(flagName)` - enable a flag
   - `disable(flagName)` - disable a flag
   - `setRollout(flagName, percentage)` - set the rollout percentage (0-100)
   - `define(flagName, config)` - define a new flag or update an existing one
   - `getFlag(flagName)` - returns the full flag config or undefined
   - `getAllFlags()` - returns all flag configs
   - `reset()` - restore all flags to their initial values

3) Rollout hashing: use a simple deterministic hash (e.g. djb2 or FNV) of `String(userId)` to get a stable 0-99 bucket; if the bucket < rolloutPercentage the flag is enabled for that user.

4) Write tests in `tests/featureFlags.test.js` covering: basic enable/disable, rollout percentage (same userId always gets same result, 100% enables for all, 0% disables for all), onChange callback, define/getFlag/getAllFlags, reset, and unknown flag returns false.

5) Export `createFlagStore` from `src/index.js` as `featureFlags`.
