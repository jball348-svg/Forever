Add a `src/di.js` module implementing a minimal Dependency Injection container. It should export `createContainer()` returning an object with: `register(name, factory)` to register a named factory function, `resolve(name)` to get an instance (calling the factory with the container so it can resolve its own dependencies), and `singleton(name, factory)` to register a factory that is only called once (subsequent resolves return the cached instance). Add `tests/di.test.js` covering basic registration, resolution, dependency chaining, and singleton behaviour. Add to npm test script.