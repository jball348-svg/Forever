Add a `src/stream.js` module implementing a simple readable/writable stream abstraction (no Node.js stream internals). It should export `createStream()` returning an object with: `write(chunk)` to push data, `read()` returning a Promise that resolves with the next chunk (queued or awaited), `end()` to signal no more data, and `onEnd` event support (a callback set via `stream.onEnd = fn`). Add `tests/stream.test.js` verifying write-then-read ordering, awaiting a future write, and end signalling. Add to npm test script.