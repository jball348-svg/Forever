Add a `src/store.js` module implementing a simple in-memory key-value store. It should export: `set(key, value)`, `get(key)` (returns undefined if missing), `has(key)`, `delete(key)`, and `clear()`. Add `tests/store.test.js` using assert to verify all five methods. Add the new test to the npm test script.