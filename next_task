Add a `src/utils.js` module with general-purpose utility functions. Include at minimum: `capitalize(str)` (uppercases first letter), `isEmpty(val)` (returns true for null/undefined/empty string/empty array/empty object), and `sleep(ms)` (returns a Promise that resolves after ms milliseconds). Add `tests/utils.test.js` covering all three functions and add it to the test script.