Add a comprehensive interactive CLI experience to the Forever library. Create `src/cli.js` and update `bin/forever.js` to support the following subcommands: 1) `forever run <module>` - dynamically require and execute any named module from src/ (e.g. `forever run cache`). 2) `forever list` - print all available modules in src/ with their one-line module summary extracted from JSDoc. 3) `forever test` - run all tests in tests/ and show a pass/fail summary with timing per test file. 4) `forever docs` - generate documentation (calls scripts/generate-docs.js programmatically). 5) `forever benchmark` - run benchmarks/index.js and display formatted timing results. 6) `forever info <module>` - show the full API of a specific module (module summary + exported function names + param signatures). Requirements: use only Node.js built-ins (no external CLI libraries). Implement coloured output using ANSI codes. Add a `--help` flag that prints usage for all commands. Add a `--version` flag. Write tests in `tests/cli.test.js` that verify each subcommand outputs the expected structure. Update `DOCUMENTATION.md` with a CLI usage guide section.