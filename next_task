Add a GitHub Actions workflow file at `.github/workflows/ci.yml` that runs the test suite automatically on every push and pull request to main. It should use the latest Ubuntu runner, set up Node.js 20, and run `npm test`. This gives the project basic continuous integration.