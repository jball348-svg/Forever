Add a comprehensive documentation generation system to the Forever library. Create `scripts/generate-docs.js` that automatically generates API documentation from JSDoc comments. Requirements: 1) Use `jsdoc` library to parse JSDoc comments from all source files in `src/`. 2) Generate HTML documentation in `docs/api/` directory with a modern, responsive theme. 3) Generate markdown documentation in `docs/api.md` that can be included in README. 4) Add support for custom JSDoc tags like `@example` and `@deprecated`. 5) Create a `docs` script in package.json that runs `node scripts/generate-docs.js`. 6) Add a `--watch` mode for continuous documentation generation during development. 7) Generate a documentation coverage report showing which functions/classes are documented. 8) Add validation to fail if documentation coverage falls below 90%. 9) Write tests in `tests/docs.test.js` that verify documentation generation works correctly. 10) Integrate with the changelog generator to include documentation changes in release notes. 11) Add JSDoc comments to all existing source files that are missing them.