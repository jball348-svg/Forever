Add a `src/commandBus.js` module implementing the Command pattern. It should export `createCommandBus()` returning an object with: `register(commandName, handler)` to bind a handler function to a command name, and `dispatch(commandName, payload)` which calls the registered handler with the payload and returns its result (throws if no handler registered). Add `tests/commandBus.test.js` verifying handler registration, dispatch with correct payload, return value propagation, and error on unknown command. Add to npm test script.