Add a GitHub Actions CI/CD pipeline to the Forever library. Create `.github/workflows/ci.yml` that runs on every push and pull request to `main`. Requirements: 1) A `test` job that runs on `ubuntu-latest` with Node.js versions 18.x, 20.x, and 22.x (matrix strategy). 2) Steps include: checkout, Node.js setup with cache, `npm install` (or `npm ci` if lock file present), then run all tests. 3) A `lint` job using `eslint` — create an `.eslintrc.json` at the repo root with sensible rules for Node.js (no-unused-vars, no-console warnings, consistent returns, etc.). 4) A `benchmark` job that runs `node benchmarks/index.js` and uploads `benchmarks/reports/` as a workflow artifact. 5) A `typecheck` job that runs `npx tsc --noEmit --strict types/example.ts` to validate TypeScript declarations. 6) Jobs should run in parallel where possible and only depend on each other where necessary. 7) Add a status badge to the `CONCEPT.md` file linking to the Actions tab.