Add a `src/validator.js` module for simple data validation. It should export a `validate(value, rules)` function where `rules` is an object with optional keys: `required` (boolean), `type` (string — 'string', 'number', 'boolean', 'array'), `minLength` (number, for strings/arrays), `maxLength` (number), `min` (number, for numbers), `max` (number). It should return `{ valid: boolean, errors: string[] }`. Add `tests/validator.test.js` with cases covering all rules. Add to npm test script.