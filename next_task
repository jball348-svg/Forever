Add a `src/memoize.js` module exporting a `memoize(fn, keyFn?)` function. It should cache the return value of fn based on its arguments (serialised to a string by default, or using an optional keyFn(args) for custom cache keys). Subsequent calls with the same arguments should return the cached value without calling fn again. Also export `memoizeAsync(fn, keyFn?)` for async functions. Add `tests/memoize.test.js` covering sync caching, cache hit counting, custom key functions, and async memoization. Add to npm test script.