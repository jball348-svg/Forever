Add a `src/middleware.js` module implementing a simple middleware chain (like Express-style middleware). It should export `createMiddleware()` which returns an object with: `use(fn)` to register a middleware function (called with `(ctx, next)` where next calls the next middleware), and `run(ctx)` to execute the chain. Add `tests/middleware.test.js` verifying that middleware runs in order, that calling next() passes control correctly, and that not calling next() stops the chain. Add to npm test script.