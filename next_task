Add a `src/router.js` module implementing a minimal hash-based client-side router (logic only, no DOM). It should export: `addRoute(path, handler)` to register a path (supports ':param' style segments), `navigate(path)` to trigger the matching handler with extracted params, and `notFound(handler)` to set a fallback. Add `tests/router.test.js` covering exact matches, param extraction, and the notFound fallback. Add to npm test script.