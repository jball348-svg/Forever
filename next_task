Add a basic test file for the greet module. Create a `tests/greet.test.js` file using Node's built-in `assert` module (no external test framework needed). It should verify that greet() returns a string, that the string is non-empty, and that it contains the word 'Forever'. Also add a `test` script to package.json that runs the test file with `node`.