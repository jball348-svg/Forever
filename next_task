Add a `tests/logger.test.js` file that tests the logger module. Since logger writes to console methods, use a simple spy pattern: temporarily replace console.log, console.warn, and console.error with custom functions that capture output, call log() with each level, assert the correct prefix and message appear, then restore the originals. No external libraries needed.