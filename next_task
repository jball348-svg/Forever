Add a `src/fsm.js` module implementing a minimal Finite State Machine. It should export `createFSM(config)` where config is `{ initial: string, states: { [state]: { on: { [event]: nextState } } } }`. The returned object should have: `state` (current state getter), `send(event)` to transition (throws if the transition is invalid), and `can(event)` to check if a transition is valid from the current state. Add `tests/fsm.test.js` covering valid transitions, invalid transitions throwing, and can() checks. Add to npm test script.