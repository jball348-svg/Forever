Now that the library has 20+ modules, update the CHANGELOG.md to document all iterations since version 0.4.0, adding entries for 0.5.0 through 0.24.0 (or similar) covering every module added: logger, config, eventBus, store, utils, pipeline, scheduler, stateManager, validator, router, cache, retry, queue, middleware, observable, di, debounce+throttle, fsm, commandBus, memoize, stream, pool, decorators. Use the standard Keep-a-Changelog format. Also bump the version in package.json to 0.5.0.